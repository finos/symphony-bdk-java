<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <property resource="application.yaml"/>
    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <Pattern>
                %date %level [%thread] %logger%replace( {%mdc}){' \{\}', ''}: %message%n%exception
            </Pattern>
        </encoder>
    </appender>
    <appender name="File" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/your_app.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <totalSizeCap>20GB</totalSizeCap>
            <maxHistory>60</maxHistory>
        </rollingPolicy>
        <append>true</append>
        <encoder>
            <Pattern>
                %date %level [%thread] %logger%replace( {%mdc}){' \{\}', ''}: %message%n%exception
            </Pattern>
        </encoder>
    </appender>

    <!-- LOG everything at INFO level -->
    <root level="${logging.level.ROOT}">
        <appender-ref ref="Console"/>
        <appender-ref ref="File"/>
    </root>

    <logger name="com.symphony.ms.devtools.your_app" additivity="false">
        <appender-ref ref="Console"/>
    </logger>
    <logger name="com.symphony.ms.devtools.your_app" additivity="false">
        <appender-ref ref="File"/>
    </logger>

</configuration>
