bdk:
  pod:
    scheme: http
    host: localhost
  agent:
    scheme: http
    host: localhost
  keyManager:
    scheme: http
    host: localhost
  bot:
    username: testbot
    privateKey.path: classpath:/privatekey.pem
  app:
    appId: testapp
    privateKey.path: classpath:/privatekey.pem

bdk-app:
  auth:
    enabled: true
    jwtCookie:
      enabled: true
      maxAge: 1d
