bdk:
  pod:
    scheme: http
    host: localhost
  agent:
    scheme: http
    host: localhost
  keyManager:
    scheme: http
    host: localhost
  bot:
    username: tibot
    privateKeyPath: classpath:/privatekey.pem
  app:
    appId: testapp
    privateKeyPath: classpath:/privatekey.pem

bdk-app:
  auth:
    enabled: true
    jwtCookie:
      enabled: true
      expireIn: 43200
