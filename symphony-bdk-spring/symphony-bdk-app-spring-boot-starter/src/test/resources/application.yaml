bdk:
  pod:
    scheme: http
    host: localhost
  agent:
    scheme: http
    host: localhost
  keyManager:
    scheme: http
    host: localhost
  bot:
    username: testbot
    privateKeyPath: classpath:/privatekey.pem
  app:
    appId: testapp
    privateKeyPath: classpath:/privatekey.pem

bdk-app:
  auth:
    enabled: true
    jwtCookie:
      enabled: true
      maxAge: 1d
