bdk:
  pod:
    scheme: http
    host: localhost

  agent:
    scheme: http
    host: localhost

  keyManager:
    scheme: http
    host: localhost

  bot:
    username: tibot
    privateKey.path: classpath:/privatekey.pem

