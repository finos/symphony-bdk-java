bdk:
  pod:
    scheme: http
    host: localhost

  agent:
    scheme: http
    host: localhost

  keyManager:
    scheme: http
    host: localhost

  bot:
    username: tibot
    privateKeyPath: classpath:/privateKey.pem

