{
    "_version": "2.0",

    "host" : "$${escaped}",

    "bot": {
        "username": "tibot",
        "privateKeyPath": "${SYMPHONY_BDK_UNIT_TEST_TEMP}/${my.property}/privatekey.pem"
    },
    "app": {
        "appId": "tibapp",
        "privateKeyPath": "${other.property:-default/value}/privatekey.pem"
    }
}
