{
    "_version": "2.0",

    "bot": {
        "username": "tibot",
        "privateKeyPath": "${my.property}/privatekey.pem"
    },
    "app": {
        "appId": "tibapp",
        "privateKeyPath": "${other.property:-default/value}/privatekey.pem"
    }
}
