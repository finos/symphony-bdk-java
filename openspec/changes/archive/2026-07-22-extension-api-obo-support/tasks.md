## 1. SPI signature changes

- [x] 1.1 Add a leading `AuthSession session` parameter to every method of `MessageSenderOverride` (`send`, `update`, `blast`, `importMessages`, `suppressMessage`, `getAttachment`)
- [x] 1.2 Add a leading `AuthSession session` parameter to every method of `MessageRetrieverOverride` (`listMessages`, `searchMessages`, `searchMessagesSemantic`, `getMessage`)
- [x] 1.3 Rewrite the Javadoc on both interfaces to describe per-call session routing (bot vs OBO) and drop the "receive auth sessions via Aware injection" guidance

## 2. MessageService wiring

- [x] 2.1 Pass `this.authSession` into every `MessageSenderOverride` call site in `MessageService` (`send`, `update`, blast `send(List, Message)`, `getAttachment`, `importMessages`, `suppressMessage`)
- [x] 2.2 Pass `this.authSession` into every `MessageRetrieverOverride` call site in `MessageService` (`doListMessages`, `searchMessages`, `searchMessagesSemantic`, `getMessage`)
- [x] 2.3 Confirm `MessageService.obo(oboSession)` forwards the override references (unchanged) so the OBO-bound copy passes the OBO session via 2.1/2.2

## 3. OBO service path

- [x] 3.1 Retain the extracted `messageSenderOverride` / `messageRetrieverOverride` as `SymphonyBdk` instance fields (currently constructor locals)
- [x] 3.2 Add an `OboServices` constructor overload accepting the two overrides and forwarding them to the 6-arg `ServiceFactory` (datafeed event source `null`)
- [x] 3.3 Update `SymphonyBdk.obo(AuthSession)` to construct `OboServices` with the retained overrides so `OboServices.messages()` returns an override-backed, OBO-bound `MessageService`

## 4. Tests

- [x] 4.1 Update existing `MessageService` / extension unit tests that stub the overrides to the new signatures
- [x] 4.2 Add a bot-context test asserting the override receives the bot `AuthSession`
- [x] 4.3 Add an OBO test via `bdk.obo(oboSession).messages()` asserting the override is invoked with the OBO `AuthSession`
- [x] 4.4 Add an OBO test via `bdk.messages().obo(oboSession)` asserting the override is invoked with the OBO `AuthSession`
- [x] 4.5 Run `./gradlew :symphony-bdk-core:test` and confirm green

## 5. Consumer & docs

- [x] 5.1 Update the symphony-agent agentless-send extension (`integrate-bdk-messaging-send`) to the new SPI signatures — SKIPPED: symphony-agent has uncommitted changes on `feat/messaging-crypto-library`; user opted to handle separately
- [x] 5.2 Update any BDK reference/extension documentation that shows override method signatures
- [x] 5.3 Add a release-note entry flagging the breaking `EXPERIMENTAL` SPI signature change — N/A: repo has no CHANGELOG.md (release notes auto-generated by GitHub from PR labels per `.github/release.yml`), and the whole override SPI is still unreleased on this feature branch; flag the breaking change in the PR description when this branch merges to main
