
bdk:
  host: acme.symphony.com
  bot:
    username: mybot
    privateKeyPath: /Users/user.name/.symphony/privatekey.pem
  app:
    appId: myapp
    privateKeyPath: /Users/user.name/.symphony/privatekey.pem

bdk-app:
  auth:
    enabled: true
    jwtCookie:
      enabled: true
      maxAge: 1d

logging:
  level:
    com.symphony: debug
